% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/importMultipleLCZvect.R
\name{importMultipleLCZvectFromSession}
\alias{importMultipleLCZvectFromSession}
\title{Take a list of sf objects within the session and format them to allow
the use of createIntersect and multipleCompare functions.}
\usage{
importMultipleLCZvectFromSession(
  sfList,
  workflowNames,
  columns,
  location,
  refCRS = 1
)
}
\arguments{
\item{sfList}{the list of LCZ sf objects}

\item{workflowNames}{a vector containing the workflow names of each sf object of sfList}

\item{columns}{contain the names of the columns where the sf objects of sfList are stored}

\item{location}{the name of the location, all files from sfList must regard the same location}

\item{refCRS}{a number telling which sf
of the sfList will be the reference in termes of Coordinate Reference System}
}
\value{
returns a list of sf objects, each of them has columns lcz_primary, wf and location
}
\description{
Take a list of sf objects within the session and format them to allow
the use of createIntersect and multipleCompare functions.
}
\examples{
osm<-importLCZvect(dirPath = paste0(
     system.file("extdata", package = "lczexplore"),"/multipleWfs/Arville"),
     file = "osm_lcz.fgb")
bdt<-importLCZvect(dirPath = paste0(
     system.file("extdata", package = "lczexplore"),"/multipleWfs/Arville"),
 file = "bdt_lcz.fgb")
wudapt<-importLCZvect(dirPath = paste0(
     system.file("extdata", package = "lczexplore"),"/multipleWfs/Arville"),
 file = "wudapt_lcz.fgb", column = "lcz_primary")
sfList<-list(osm = osm, bdt = bdt, wudapt = wudapt)
sfListFormatted<- importMultipleLCZvectFromSession(sfList = sfList,
workflowNames = c("osm", "bdt", "wudapt"),location = "Arville",
columns = c("LCZ_PRIMARY", "LCZ_PRIMARY", "lcz_primary" ))
intersected<-createIntersect(sfList = sfListFormatted, columns = rep("lcz_primary", 3),
workflowNames = c("osm", "bdt", "wudapt"))
multicompare_test<-compareMultipleLCZ(intersected,
columns = c("osm","bdt","wudapt"),trimPerc = 0.5)
}
