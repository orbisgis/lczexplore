% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplotLCZfromIntersect.R
\name{barplotLCZfromIntersect}
\alias{barplotLCZfromIntersect}
\title{plots the repartition of LCZ types regarding of their source (map/workflow)
from an sf object containing all spatial units and LCZ value, typically the output of createIntersect}
\usage{
barplotLCZfromIntersect(
  sfIn,
  workflowNames = NULL,
  columns = NULL,
  stat = "perc",
  plotNow = TRUE
)
}
\arguments{
\item{sfIn}{is the file containing the spatial units and LCZ type per workflow}

\item{workflowNames}{is a vector of prefixes. The LCZ files must be named workflow_rsu.fgb
where workflow is on of the values in workflowNames vector}

\item{columns}{is the vector of names of the columns containing the LCZ types. If not specified, the function
will try to use the workflowNames instead}

\item{stat}{can take the value "perc" to plot percentage of area for each LCZ type or "sum" for total
area for each LCZ type}

\item{plotNow}{If TRUE, the boxplot of the repartition will be printed}
}
\value{
A barplot of LCZ area percentage by LCZ type and workflow
}
\description{
plots the repartition of LCZ types regarding of their source (map/workflow)
from an sf object containing all spatial units and LCZ value, typically the output of createIntersect
}
\examples{

# Two Locations
sfList2<-loadMultipleLocsSfs(
 dirPath = paste0(
     system.file("extdata", package = "lczexplore"),
     "/multipleWfs"),
 workflowNames = c("osm","bdt","wudapt"))

twoLocsIntersect <- createIntersect(
 sfList = sfList2, columns = rep("lcz_primary", 3),
 workflowNames = c("osm","bdt","wudapt"))
example<-barplotLCZfromIntersect(sfIn = twoLocsIntersect,
                              columns = c("osm", "bdt", "wudapt"),
                            workflowNames = c("osm", "bdt", "wudapt"))
}
